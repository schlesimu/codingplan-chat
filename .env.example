# 小纸船 / codingplan-chat 环境变量示例
#
# ⚠️ 不要把真实 API key 写进源码、README、Issue、截图或 commit 历史。
# 在 Cloudflare Pages 控制台里配置这些变量：
# Workers & Pages → codingplan-chat → Settings → Environment variables

# 火山方舟 / CodingPlan API Key
# 用于 /api/chat 与 /api/models 的默认服务端 key。
# 用户也可以在前端设置自己的 key，通过 X-Codingplan-Key 覆盖。
ARK_API_KEY=ark-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 博查 AI Search API Key
# 用于 /api/search 的默认服务端 key。
# 用户也可以在前端设置自己的搜索 key，通过 X-Search-Key 覆盖。
BOCHA_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Cloudflare KV 绑定名（不是普通文本变量）
# 在 Pages → Settings → Functions → KV namespace bindings 中绑定：
# Variable name: CODINGPLAN_KV
# KV namespace: 选择你创建的 KV namespace
CODINGPLAN_KV=your-kv-binding-name
